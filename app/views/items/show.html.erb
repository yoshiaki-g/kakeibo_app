<div class='item-contents'>
    <h2 class='title'>支出</h2>
    <%= link_to 'ホーム画面に戻る', "#", class: "subtitle" %>
    <ul class='item-lists'>

     <% @items.each do |item| %>
      <%# 商品のインスタンス変数になにか入っている場合、中身のすべてを展開できるようにしましょう %>
      <li class='list'>
        <%= link_to item_path(item.id), method: :get do %>
        
        <div class='item-info'>
          <h3 class='item-name'>
            <%= item.category.name %>
          </h3>
          <div class='item-price'>
            <span><%= item.value %>円<br>(税込み)</span>
          </div>
        </div>
     <% end %>
      </li>
      <%# //商品のインスタンス変数になにか入っている場合、中身のすべてを展開できるようにしましょう %>
     <% end %>
    </ul>
  </div>